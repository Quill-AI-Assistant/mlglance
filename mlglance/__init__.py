"""mlglance — a terminal-native live training monitor.

Watch any trainer's metrics JSONL stream as a glanceable, auto-sizing dashboard:
loss curves, a validation-driven verdict (converging / overfitting / diverging),
ETA, throughput and resource health — no browser, no server, ssh/tmux friendly.
"""
__version__ = "0.1.0"
